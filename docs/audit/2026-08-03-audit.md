# mcpx Plugins, Skills, and Desktop Audit

**Date:** 2026-08-03

## Fixed

- Validated shared skill identifiers before filesystem access.
- Added containment checks for skill and plugin projection reads and deletes.
- Normalized plugin names before using them as data and projection directories.
- Added defensive ownership-manifest validation.
- Validated plugin HTTP URLs before committing them to configuration.
- Hardened plugin-host path resolution and child-process startup errors.
- Fixed plugin enable/disable server state and plugin preparation by name.
- Fixed Plugins tab persistence and skill save/delete synchronization.
- Added confirmation and error handling to server removal and replaced plugin native confirms.
- Fixed cursor projection pruning, stale projection cleanup, and namespaced projection duplication.
- Fixed GitHub-prefixed sources and unknown source errors.
- Converted plugin acquisition, marketplace acquisition, lifecycle preparation, and CLI update commands away from blocking shell execution.
- Added update-check error and timeout handling.
- Added typed preload results for status, skills, plugins, marketplaces, daemon, and sync calls.
- Fixed dashboard development loading and update lock timer lifetime.

## Deferred

- Cross-process locking for the encrypted secrets store and OAuth refresh rotation.
- Daemon restart/fd lifecycle races.
- Full IPC zod parsing at every renderer trust boundary.
- Barrel export reduction and unrelated dead-code removal.
- Marketplace fetch timeouts and lock recursion cleanup.

## Verification

The CLI and desktop app builds pass after the fixes. The existing CLI and desktop test suites remain the regression gate; targeted plugin, skills, IPC, and renderer tests were run during implementation.
