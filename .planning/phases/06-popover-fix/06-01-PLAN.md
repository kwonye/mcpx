---
phase: 06-popover-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/renderer/index.css
  - app/src/renderer/components/StatusPopover.tsx
  - app/test/components/StatusPopover.test.tsx
autonomous: true
requirements:
  - POPOVER-01
  - POPOVER-02
user_setup: []
must_haves:
  truths:
    - "User can scroll popover content when there are many servers"
    - "Popover header remains visible while scrolling content"
    - "Each action button appears exactly once in the popover (no duplicates)"
  artifacts:
    - path: "app/src/renderer/index.css"
      provides: "Popover scrollable CSS"
      contains: ".popover {"
      min_lines: 1
    - path: "app/src/renderer/components/StatusPopover.tsx"
      provides: "Popover UI component"
      exports: ["StatusPopover"]
    - path: "app/test/components/StatusPopover.test.tsx"
      provides: "Popover component tests"
  key_links:
    - from: "StatusPopover.tsx"
      to: "index.css"
      via: "className='popover'"
      pattern: "className.*popover"
---

<objective>
Fix menu bar popover so content is scrollable and buttons are organized without duplicates.

Purpose: The popover currently has scroll events blocked by `-webkit-app-region: drag` and has duplicate daemon controls (header power icon and footer would have separate toggle). This phase removes the drag CSS and reorganizes buttons for a cleaner UI.

Output: Scrollable popover with two footer buttons: "Open Dashboard" (primary) and daemon toggle (secondary).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## User Decisions (LOCKED - implement exactly as specified)

From `.planning/phases/06-popover-fix/06-CONTEXT.md`:

- **D-01:** Remove `-webkit-app-region: drag` from `.popover` CSS class entirely
- **D-03:** Remove all header icon buttons (settings and power icons)
- **D-04:** Keep footer section with two buttons: "Open Dashboard" (primary styling) and daemon toggle button (secondary styling)
- **D-05:** Remove "Sync All Clients" button from popover
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove scroll-blocking CSS from popover</name>
  <files>app/src/renderer/index.css</files>
  <read_first>
    - app/src/renderer/index.css (lines 416-510 for popover CSS)
  </read_first>
  <action>
    Per D-01: Remove the `-webkit-app-region: drag` property from the `.popover` class that blocks scroll events.

    Specific changes:
    1. Remove line `-webkit-app-region: drag;` (line 423) from `.popover` class
    2. Remove `-webkit-app-region: no-drag;` from `.popover-header` class (line 432) — no longer needed
    3. Remove `-webkit-app-region: no-drag;` from `.popover-summary` class (line 452) — no longer needed
    4. Remove `-webkit-app-region: no-drag;` from `.popover-actions` class (line 476) — no longer needed

    After removal, the `.popover` class should only have: display, flex-direction, height, padding, gap.
  </action>
  <acceptance_criteria>
    - `app/src/renderer/index.css` does NOT contain `-webkit-app-region: drag`
    - `app/src/renderer/index.css` does NOT contain `-webkit-app-region: no-drag`
    - `.popover` class still exists with display, flex-direction, height, padding, gap properties
    - `npm test -- --filter=StatusPopover` passes in app directory
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/will/Developer/kwonye/mcpx/app && npm test -- --run 2>&1 | head -50</automated>
  </verify>
  <done>
    CSS no longer blocks scroll events. Popover content is scrollable via CSS overflow on the main element.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reorganize popover buttons</name>
  <files>app/src/renderer/components/StatusPopover.tsx, app/test/components/StatusPopover.test.tsx</files>
  <read_first>
    - app/src/renderer/components/StatusPopover.tsx (current implementation)
    - app/test/components/StatusPopover.test.tsx (existing tests)
  </read_first>
  <behavior>
    - Test 1: "Open Dashboard" button appears in footer (not duplicate in header)
    - Test 2: Daemon toggle button appears in footer with correct text based on daemon state
    - Test 3: No settings icon button exists in header
    - Test 4: No power icon button exists in header
    - Test 5: No "Sync All Clients" button exists in footer
  </behavior>
  <action>
    Per D-03, D-04, D-05: Remove header icon buttons and reorganize footer buttons.

    In `app/src/renderer/components/StatusPopover.tsx`:

    1. REMOVE the header buttons div (lines 48-57):
       - Delete the settings icon button (onClick opens dashboard)
       - Delete the power icon button (onClick toggles daemon)

    2. In the footer (lines 99-107), replace "Sync All Clients" button with daemon toggle button:
       - Keep "Open Dashboard" button as primary (line 100-103)
       - REPLACE the "Sync All Clients" button (lines 104-106) with:
         ```tsx
         <button className="popover-btn" onClick={handleDaemonToggle}>
           {report.daemon.running ? "Stop Daemon" : "Start Daemon"}
         </button>
         ```

    3. Clean up the header div wrapper since buttons are removed — keep only the title text.

    In `app/test/components/StatusPopover.test.tsx`:

    1. Remove test "shows Stop Daemon when running" (lines 50-53) — tests header icon which is removed
    2. Remove test "shows Start Daemon when stopped" (lines 55-64) — tests header icon which is removed
    3. Add new test: "shows daemon toggle button in footer when running" — check for button with text "Stop Daemon"
    4. Add new test: "shows daemon toggle button in footer when stopped" — check for button with text "Start Daemon"
    5. Ensure "keeps the Open Dashboard action available" test still passes (button text, not icon)

    The footer should now have exactly two buttons:
    - "Open Dashboard" (primary styling, opens dashboard)
    - "Stop Daemon" or "Start Daemon" (secondary styling, toggles daemon state)
  </action>
  <acceptance_criteria>
    - StatusPopover.tsx does NOT contain `settings` material symbol
    - StatusPopover.tsx does NOT contain `power_settings_new` material symbol
    - StatusPopover.tsx does NOT contain `Sync All Clients`
    - StatusPopover.tsx contains `Stop Daemon` string for toggle button text
    - StatusPopover.tsx contains `Start Daemon` string for toggle button text
    - StatusPopover.tsx footer has exactly 2 buttons (primary + secondary)
    - `npm test -- --run` passes in app directory with all StatusPopover tests passing
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/will/Developer/kwonye/mcpx/app && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    Popover has clean header with title only. Footer has "Open Dashboard" and daemon toggle buttons. No duplicate functionality. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. Run `npm test -- --run` in app directory — all tests pass
2. Build app with `npm run build` — no build errors
3. Manual verification: Open popover, verify header has title only, footer has two buttons, content scrolls when servers overflow
</verification>

<success_criteria>
1. User can scroll popover content when there are many servers (CSS `-webkit-app-region: drag` removed)
2. Popover header remains visible while scrolling content (header is outside the scrollable main element)
3. Each action button appears exactly once in the popover:
   - "Open Dashboard" button appears only in footer (not in header as settings icon)
   - Daemon toggle appears only in footer (not in header as power icon)
   - "Sync All Clients" button is removed
</success_criteria>

<output>
After completion, create `.planning/phases/06-popover-fix/06-01-SUMMARY.md`
</output>