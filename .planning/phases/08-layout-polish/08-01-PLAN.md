---
phase: 08-layout-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/renderer/components/Dashboard.tsx
  - app/src/renderer/components/DaemonControls.tsx
  - app/src/renderer/index.css
autonomous: true
requirements:
  - SIDE-01
  - WIND-02

must_haves:
  truths:
    - "Daemon controls appear at the top of the sidebar, below the logo"
    - "Daemon controls are no longer in the main content servers-controls-container"
    - "All dashboard padding uses consistent 16px values"
  artifacts:
    - path: "app/src/renderer/components/Dashboard.tsx"
      provides: "Dashboard layout with DaemonControls in sidebar"
      contains: "<DaemonControls"
    - path: "app/src/renderer/index.css"
      provides: "Standardized padding values throughout dashboard"
      contains: ".nav-button"
  key_links:
    - from: "Dashboard.tsx"
      to: "DaemonControls"
      via: "import and JSX placement"
      pattern: "DaemonControls.*daemon.*onRefresh"
---

<objective>
Move daemon controls to sidebar hero position and standardize all dashboard padding to macOS-standard 16px.

Purpose: Daemon controls become the most prominent action in the UI, and consistent padding creates a polished, native feel.
Output: Updated Dashboard.tsx with reorganized sidebar, DaemonControls styled for sidebar context, and standardized CSS padding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-layout-polish/08-CONTEXT.md
</context>

<interfaces>
<!-- Key interfaces and types the executor needs -->

From app/src/renderer/components/DaemonControls.tsx:
```typescript
interface DaemonControlsProps {
  daemon: { running: boolean; pid?: number; port?: number };
  onRefresh: () => void;
}
```

From app/src/renderer/hooks/useMcpx.ts (used in Dashboard):
```typescript
const { status, loading, refresh } = useStatus();
// status.daemon provides the daemon state
// refresh is the callback to refresh status
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move DaemonControls to sidebar hero position (SIDE-01)</name>
  <files>app/src/renderer/components/Dashboard.tsx, app/src/renderer/components/DaemonControls.tsx</files>
  <read_first>
    - app/src/renderer/components/Dashboard.tsx (current structure)
    - app/src/renderer/components/DaemonControls.tsx (component props and styling)
    - .planning/phases/08-layout-polish/08-CONTEXT.md (D-01 through D-05 decisions)
  </read_first>
  <action>
    Per D-01: Move DaemonControls component to top of sidebar, below `.sidebar-logo` and above the nav buttons container.

    Per D-02: Daemon controls becomes the "hero" element in sidebar - most prominent position.

    Per D-03: Remove DaemonControls from `servers-controls-container` in main content (line 95 of current Dashboard.tsx).

    Per D-04: Style daemon controls for sidebar context - compact, glass-panel styling.

    Per D-05: CliCommandInput remains in main content area (servers-controls-container).

    **Dashboard.tsx changes:**
    1. Move the `<DaemonControls daemon={report.daemon} onRefresh={refresh} />` line from inside `servers-controls-container` to inside the sidebar, after `.sidebar-logo` div, before `.sidebar-inner`.
    2. The DaemonControls needs to be rendered between the logo and the sidebar-inner div.
    3. Remove DaemonControls from the servers-controls-container div (keep only CliCommandInput there).

    **DaemonControls.tsx changes:**
    1. Update inline styles for sidebar context:
       - Remove `marginBottom: "24px"` (sidebar has its own spacing)
       - Change padding from `16px 20px` to `12px 16px` (more compact for sidebar)
       - Keep `borderRadius: "16px"` and glass-panel class

    The sidebar structure after change should be:
    ```
    <aside className="sidebar">
      <div className="sidebar-logo">...</div>
      <DaemonControls daemon={report.daemon} onRefresh={refresh} />
      <div className="sidebar-inner glass-panel">
        <button>My Servers</button>
        ...
      </div>
    </aside>
    ```
  </action>
  <verify>
    <automated>grep -q "DaemonControls" app/src/renderer/components/Dashboard.tsx && grep -A5 "sidebar-logo" app/src/renderer/components/Dashboard.tsx | grep -q "DaemonControls"</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.tsx contains `<DaemonControls` inside the sidebar, after `sidebar-logo` div
    - Dashboard.tsx no longer has `<DaemonControls` inside `servers-controls-container`
    - DaemonControls.tsx has `padding: "12px 16px"` in the glass-panel div style
  </acceptance_criteria>
  <done>
    - DaemonControls appears in sidebar between logo and nav buttons
    - DaemonControls removed from main content servers-controls-container
    - CliCommandInput remains in servers-controls-container
    - DaemonControls has compact styling appropriate for sidebar
  </done>
</task>

<task type="auto">
  <name>Task 2: Standardize dashboard padding to 16px (WIND-02)</name>
  <files>app/src/renderer/index.css</files>
  <read_first>
    - app/src/renderer/index.css (current padding values)
    - .planning/phases/08-layout-polish/08-CONTEXT.md (D-06 through D-08 decisions)
  </read_first>
  <action>
    Per D-06: Standardize all dashboard padding to 16px (macOS standard).

    Per D-08: Make these specific changes to index.css:

    1. `.sidebar` (line 261): Already has `padding: 16px` - KEEP AS IS

    2. `.sidebar-logo` (line 282): Has `padding: 8px` - KEEP AS IS (smaller is appropriate for logo area per D-08)

    3. `.nav-button` (line 320): Change from `padding: 10px 12px` to `padding: 12px 16px`

    4. `.page-header` (line 351): Change from `padding: 16px 24px 16px 0` to `padding: 16px` (remove the 24px right padding and 0 left padding inconsistency)

    5. `.popover` (line 432): Already has `padding: 16px` - KEEP AS IS

    Also update main-content padding:
    - `.main-content` (line 351): Currently has `padding: 16px 24px 16px 0` - change to `padding: 16px`

    The goal is consistent 16px padding throughout, with only the logo area retaining smaller 8px padding as appropriate.
  </action>
  <verify>
    <automated>grep -E "\.nav-button\s*\{" -A5 app/src/renderer/index.css | grep -q "padding: 12px 16px"</automated>
  </verify>
  <acceptance_criteria>
    - `.nav-button` has `padding: 12px 16px`
    - `.page-header` has `padding: 16px` (not `16px 24px 16px 0`)
    - `.main-content` has `padding: 16px` (not `16px 24px 16px 0`)
    - No padding values of 10px, 20px, or 24px in dashboard-related CSS classes
  </acceptance_criteria>
  <done>
    - All dashboard containers use consistent 16px padding
    - .nav-button uses 12px 16px padding
    - .page-header uses uniform 16px padding
    - .main-content uses uniform 16px padding
    - Only .sidebar-logo retains smaller 8px padding
  </done>
</task>

</tasks>

<verification>
1. Open dashboard in desktop app
2. Verify daemon controls appear in sidebar between logo and nav buttons
3. Verify daemon controls have compact styling
4. Verify consistent padding throughout dashboard
5. Verify no visual regressions in sidebar or main content
</verification>

<success_criteria>
- Daemon controls visible in sidebar hero position
- Daemon controls removed from main content area
- All padding values standardized to 16px
- No uneven margins or spacing visible
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-polish/08-01-SUMMARY.md`
</output>