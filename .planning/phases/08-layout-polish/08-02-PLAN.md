---
phase: 08-layout-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/renderer/components/BrowseTab.tsx
  - app/src/renderer/components/CliCommandInput.tsx
  - app/src/renderer/index.css
autonomous: true
requirements:
  - BROWSE-01
  - PASTE-01

must_haves:
  truths:
    - "Browse registry displays cards in a 2-column grid layout"
    - "Cards maintain consistent 16px gap between them"
    - "Paste command placeholder displays as readable multi-line text"
    - "User can paste long commands without horizontal scrolling"
  artifacts:
    - path: "app/src/renderer/components/BrowseTab.tsx"
      provides: "Grid-based server card layout"
      contains: "display: grid"
    - path: "app/src/renderer/components/CliCommandInput.tsx"
      provides: "Multi-line paste command display"
      contains: "placeholder"
    - path: "app/src/renderer/index.css"
      provides: "CSS Grid rules for browse cards"
      contains: ".browse-grid"
  key_links:
    - from: "BrowseTab.tsx"
      to: "index.css"
      via: "CSS class for grid layout"
      pattern: "className.*browse"
    - from: "CliCommandInput.tsx"
      to: "placeholder text"
      via: "input element"
      pattern: "placeholder=.*mcp.*add"
---

<objective>
Change browse registry to grid layout and improve paste command display with multi-line placeholder.

Purpose: Better use of horizontal space in browse view, and more readable command examples in paste input.
Output: Grid-based BrowseTab with responsive layout, CliCommandInput with improved placeholder display.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-layout-polish/08-CONTEXT.md
</context>

<interfaces>
<!-- Key interfaces from existing code -->

From app/src/renderer/components/BrowseTab.tsx:
```typescript
interface BrowseTabProps {
  onServerAdded: () => void;
  status: {
    servers: Array<{ name: string }>;
  };
}
```

From app/src/renderer/components/CliCommandInput.tsx:
```typescript
interface CliCommandInputProps {
  onServerAdded: () => void;
}
```

The server cards container is currently at line 167:
```jsx
<div style={{ display: "flex", flexDirection: "column", gap: "12px", paddingBottom: "48px" }}>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Change browse registry to 2-column grid layout (BROWSE-01)</name>
  <files>app/src/renderer/components/BrowseTab.tsx, app/src/renderer/index.css</files>
  <read_first>
    - app/src/renderer/components/BrowseTab.tsx (current card container layout at line 167)
    - app/src/renderer/index.css (existing CSS patterns)
    - .planning/phases/08-layout-polish/08-CONTEXT.md (D-09 through D-13 decisions)
  </read_first>
  <action>
    Per D-09: Change from single-column list to 2-column grid layout.

    Per D-10: Use CSS Grid with `grid-template-columns: repeat(2, 1fr)`.

    Per D-11: Consistent 16px gap between cards.

    Per D-12: Cards maintain current styling (glass-card, 16px 20px padding).

    Per D-13: On narrower windows, collapse to single column via `minmax(300px, 1fr)`.

    **BrowseTab.tsx changes:**
    Find the server cards container div at approximately line 167:
    ```jsx
    <div style={{ display: "flex", flexDirection: "column", gap: "12px", paddingBottom: "48px" }}>
    ```

    Change to use CSS Grid with a class name:
    ```jsx
    <div className="browse-grid">
    ```

    **index.css additions:**
    Add a new CSS class for the browse grid:
    ```css
    /* Browse Registry Grid Layout */
    .browse-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 16px;
        padding-bottom: 48px;
    }
    ```

    The `minmax(320px, 1fr)` pattern makes the grid responsive:
    - Wide windows: 2+ columns
    - Narrower windows: automatically collapses to 1 column

    Keep the individual card styling as-is (glass-card class with inline styles for padding, border-radius, etc.).
  </action>
  <verify>
    <automated>grep -q "browse-grid" app/src/renderer/index.css && grep -q "browse-grid" app/src/renderer/components/BrowseTab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - index.css contains `.browse-grid` class with `display: grid`
    - index.css contains `.browse-grid` with `grid-template-columns: repeat(auto-fill, minmax(320px, 1fr))`
    - index.css contains `.browse-grid` with `gap: 16px`
    - BrowseTab.tsx uses `className="browse-grid"` on the server cards container
    - BrowseTab.tsx no longer has inline flex styling on the server cards container
  </acceptance_criteria>
  <done>
    - Browse registry displays cards in a responsive 2-column grid
    - Grid collapses to single column on narrower windows
    - 16px gap between cards
    - Individual card styling unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Improve paste command placeholder display (PASTE-01)</name>
  <files>app/src/renderer/components/CliCommandInput.tsx</files>
  <read_first>
    - app/src/renderer/components/CliCommandInput.tsx (current placeholder at line 46)
    - .planning/phases/08-layout-polish/08-CONTEXT.md (D-14 through D-17 decisions)
  </read_first>
  <action>
    Per D-14: The placeholder text shows a long command example - wrap it to multiple lines.

    Per D-15: Change input styling to allow text wrap in placeholder.

    Per D-16: Consider showing example commands in a help text below the input instead of in placeholder.

    Per D-17: Keep single-line input for actual user paste - users paste, not type.

    **CliCommandInput.tsx changes:**

    The current placeholder is at line 46:
    ```
    placeholder="e.g., claude mcp add slack --transport http https://mcp.slack.com/mcp"
    ```

    Change approach: Instead of trying to wrap placeholder text (which doesn't work well in single-line inputs), move the example to the help text below the input.

    1. Change the placeholder to something shorter and more actionable:
    ```
    placeholder="Paste your mcpx add command here..."
    ```

    2. Update the help text (currently at line 66-68) to include an example on a new line:
    ```
    <p className="field-description" style={{ marginTop: "4px" }}>
      Supports: <code style={{ background: "rgba(0,0,0,0.05)", padding: "2px 6px", borderRadius: "4px" }}>claude mcp add</code>, <code style={{ background: "rgba(0,0,0,0.05)", padding: "2px 6px", borderRadius: "4px" }}>codex mcp add</code>, <code style={{ background: "rgba(0,0,0,0.05)", padding: "2px 6px", borderRadius: "4px" }}>qwen mcp add</code>, <code style={{ background: "rgba(0,0,0,0.05)", padding: "2px 6px", borderRadius: "4px" }}>code --add-mcp</code>, <code style={{ background: "rgba(0,0,0,0.05)", padding: "2px 6px", borderRadius: "4px" }}>mcpx add</code>
      <br />
      <span style={{ color: "var(--text-muted)", fontSize: "11px" }}>Example: claude mcp add slack --transport http https://mcp.slack.com/mcp</span>
    </p>
    ```

    This approach:
    - Shortens the placeholder to be actionable
    - Moves the long example to help text where it can wrap naturally
    - Keeps the input single-line for pasting (per D-17)

    Also per D-08: Update glass-panel padding from 20px to 16px:
    The glass-panel div at line 33 has `padding: "20px"` - change to `padding: "16px"`.
  </action>
  <verify>
    <automated>grep -q "Paste your mcpx add command" app/src/renderer/components/CliCommandInput.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CliCommandInput placeholder text is short: "Paste your mcpx add command here..."
    - CliCommandInput has example command in the help text below the input
    - CliCommandInput glass-panel has `padding: "16px"` (not 20px)
    - Input remains single-line (no textarea)
  </acceptance_criteria>
  <done>
    - Placeholder is short and actionable
    - Example command moved to help text for natural wrapping
    - Input remains paste-friendly single-line
    - Padding standardized to 16px
  </done>
</task>

</tasks>

<verification>
1. Open browse tab in desktop app
2. Verify server cards display in 2-column grid on wide window
3. Verify grid collapses to 1 column on narrow window
4. Open servers tab and verify paste command input
5. Verify placeholder is short and readable
6. Verify example appears in help text below input
</verification>

<success_criteria>
- Browse registry shows organized 2-column grid layout
- Grid responds to window size changes
- Paste command placeholder is short and actionable
- Example command visible in help text, wraps naturally
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-polish/08-02-SUMMARY.md`
</output>